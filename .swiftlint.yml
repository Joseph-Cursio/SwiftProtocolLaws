included:
  - Sources
  - Tests

excluded:
  - .build
  - Validation/.build
  - docs
  - Package.swift

# Idiomatic two-letter identifier name we use; everything else complies
# with the default 3-character minimum.
identifier_name:
  excluded:
    - id
